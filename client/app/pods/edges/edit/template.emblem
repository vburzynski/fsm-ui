h2 Edit edge

.form-horizontal
  .form-group
    label.col-sm-2.control-label Title
    .col-sm10
      = input type="text" value=model.title class="form-control" placeholder="Edge's Title"

  .form-group
    .col-sm-offset-2.col-sm-10
      button{action "saveEdge" model} type="submit" class="btn btn-default" Save

= outlet
